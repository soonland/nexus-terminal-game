# AI API keys for Phase 3 — copy to .env.local and fill in values
# .env.local is gitignored and never committed

# Groq — used by /api/world
GROQ_API_KEY=

# Google Gemini — used by /api/file and /api/aria
GEMINI_API_KEY=
